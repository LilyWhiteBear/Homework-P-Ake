import { StreamingLoginDirective } from './streaming-login.directive';

describe('StreamingLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new StreamingLoginDirective();
    expect(directive).toBeTruthy();
  });
});
