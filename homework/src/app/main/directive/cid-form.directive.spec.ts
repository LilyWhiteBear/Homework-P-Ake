import { CidFormDirective } from './cid-form.directive';

describe('CidFormDireectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CidFormDirective();
    expect(directive).toBeTruthy();
  });
});
