import { OtpFillerDirective } from './otp-filler.directive';

describe('OtpFillerDirective', () => {
  it('should create an instance', () => {
    const directive = new OtpFillerDirective();
    expect(directive).toBeTruthy();
  });
});
