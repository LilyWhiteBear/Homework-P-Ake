import { EmailFormDirective } from './email-form.directive';

describe('EmailFormDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailFormDirective();
    expect(directive).toBeTruthy();
  });
});
