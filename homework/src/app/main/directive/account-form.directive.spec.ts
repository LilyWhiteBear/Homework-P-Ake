import { AccountFormDirective } from './account-form.directive';

describe('AccountFormDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountFormDirective();
    expect(directive).toBeTruthy();
  });
});
