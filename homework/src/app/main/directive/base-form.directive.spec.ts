import { BaseFormDirective } from './base-form.directive';

describe('BaseFormDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseFormDirective();
    expect(directive).toBeTruthy();
  });
});
